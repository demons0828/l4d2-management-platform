# L4D2管理平台前端Dockerfile

FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY ../frontend/package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY ../frontend .

# 构建应用
RUN npm run build

# 安装serve用于生产环境
RUN npm install -g serve

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["serve", "-s", "dist", "-l", "3000"]
